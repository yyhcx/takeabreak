{
  "name": "takeabreak",
  "version": "0.1.0",
  "private": true,
  "description": "A lightweight trend board for taking a quick break.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check api/*.js"
  },
  "engines": {
    "node": ">=20"
  }
}
